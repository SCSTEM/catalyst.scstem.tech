{
  "permissions": {
    "allow": [
      "WebSearch",
      "Bash(bun run typecheck:*)",
      "Bash(bun run build:*)",
      "Bash(bun run check:*)",
      "Bash(bun run test:*)",
      "Bash(bun run db:migrate:local:*)"
    ]
  },
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "bun run check && bun run typecheck"
          }
        ]
      }
    ]
  }
}
